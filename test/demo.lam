@include "lib/church"

let factorial = fix (\fct. \n. (equal n c0) c1 (times n (fct (prd n))))

# Evaluate factorial of 1 with step output
@step
realnat (factorial c1)

# Evaluate factorial of 6
@eval
realnat (factorial (churchnat 6))
